

CKEDITOR.editorConfig = function( config )
{
         config.extraPlugins = 'newplugin,footnote';
	// Define changes to default configuration here. For example:
	// config.language = 'fr';
	// config.uiColor = '#AADC6E';
};
