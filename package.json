{
  "name": "Varaa",
  "description": "The main repo of Varaa.com",
  "engine": "node >= 0.10",
  "private": true,
  "devDependencies": {
    "coffee-script": "^1.8.0",
    "gulp": "^3.8.10",
    "gulp-cached": "^1.0.4",
    "gulp-coffee": "^2.2.0",
    "gulp-concat": "^2.4.2",
    "gulp-cssmin": "^0.1.6",
    "gulp-if": "^1.2.5",
    "gulp-less": "^3.0.3",
    "gulp-remember": "^0.3.0",
    "gulp-rev": "^3.0.1",
    "gulp-uglify": "^1.0.1",
    "less": "^2.5.0",
    "lodash": "^2.4.1",
    "merge-stream": "^0.1.7",
    "yargs": "^3.7.2"
  },
  "scripts": {
    "clean-build": "rm rev.json && rm public/a/* -r",
    "build": "mkdir -p resources/$ENV && cp -u -r resources/varaa/* resources/$ENV && npm run clean-build && gulp --env $ENV --production"
  }
}
